{
  "name": "<%= moduleName %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "scripts": {
    "test": "mocha test/*.js"
  },
  "repository": "<%= repository %>",
  "dependencies": {},
  "devDependencies": {
    "mocha": "~1.21.x"
  }
}

